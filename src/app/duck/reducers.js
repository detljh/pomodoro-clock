import types from './types.js';

const INITIAL_STATE = {
    
};

const homeReducer = (state=INITIAL_STATE, action) => {
    switch (action.type) {
        default:       
            return state;
    };
};

export default homeReducer;